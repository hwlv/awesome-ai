1. 支持双击打开lbtb3,czb和cys文件调起鲁班编标工具打开文件
2. 共同协议下 字段域的是否可以点击需要特殊处理。需要识别字段域所属的章节。
3. 更新答疑图片资源合并
4. 当前项目安装的时候，把快捷键加到开始菜单里面去
5. xml 替换值时默认宋体,包括占位字符。
6. bug修复。插入pdf多余空白。动态章节和更新答疑冲突问题。
7. 优化。样式修复，banner 上下间距，去掉伪按钮元素，新建工程调色。优化清空workspace逻辑。字段域原始占位加花括号。编辑器预加载，防止白屏
